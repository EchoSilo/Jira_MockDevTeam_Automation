---
phase: 01-time-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/state/models.py
  - src/main.py
  - src/orchestrator/orchestrator.py
  - src/orchestrator/analyzer.py
  - src/state/simulation_state.py
  - src/services/jira_client.py
  - src/agents/coordinator_agent.py
  - src/agents/release_manager_agent.py
  - src/logging/models.py
  - src/logging/writer.py
  - src/logging/database.py
  - src/scenarios/sprint_scenario.py
autonomous: true

must_haves:
  truths:
    - "No file in src/ contains datetime.utcnow() calls"
    - "No file in src/ contains naive datetime.now() calls (without timezone)"
    - "All Pydantic models use pendulum.now('UTC') in default_factory"
    - "Clock is injected into ScenarioOrchestrator"
    - "Tests can use FakeClock to control time"
  artifacts:
    - path: "src/state/models.py"
      provides: "Pydantic models with pendulum datetime defaults"
      contains: "pendulum.now"
    - path: "src/orchestrator/orchestrator.py"
      provides: "Orchestrator with Clock injection"
      contains: "clock: Clock"
  key_links:
    - from: "src/orchestrator/orchestrator.py"
      to: "src/time/clock.py"
      via: "constructor injection"
      pattern: "from src\\.time import"
    - from: "src/main.py"
      to: "src/time/clock.py"
      via: "get_clock dependency"
      pattern: "from src\\.time import"
---

<objective>
Migrate all datetime.utcnow() and datetime.now() calls to use the Clock abstraction or pendulum.now("UTC") for timezone-aware UTC handling throughout the codebase.

Purpose: Replace 40+ scattered direct time calls with either injected Clock (for testable code paths) or pendulum.now("UTC") (for Pydantic defaults and one-off timestamps). This ensures consistent timezone-aware datetime handling and enables deterministic testing.

Output: All source files updated to use pendulum or Clock. No naive datetime calls remaining. Clock injected into orchestrator and main endpoints.
</objective>

<execution_context>
@C:\Users\Jamal\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Jamal\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-time-infrastructure/01-RESEARCH.md
@.planning/phases/01-time-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-time-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Pydantic model defaults to pendulum</name>
  <files>
    - src/state/models.py
    - src/logging/models.py
    - src/scenarios/sprint_scenario.py
  </files>
  <action>
Add `import pendulum` at the top of each file.

**In src/state/models.py**, replace all `datetime.utcnow` with `pendulum.now("UTC")`:

1. Line ~132 ReleaseDirective:
   - `created_at: datetime = Field(default_factory=datetime.utcnow)`
   - Change to: `created_at: datetime = Field(default_factory=lambda: pendulum.now("UTC"))`

2. Line ~214 ActionRecord:
   - `timestamp: datetime = Field(default_factory=datetime.utcnow)`
   - Change to: `timestamp: datetime = Field(default_factory=lambda: pendulum.now("UTC"))`

3. Line ~236 ActiveScenario (started field):
   - `started: datetime = Field(default_factory=datetime.utcnow)`
   - Change to: `started: datetime = Field(default_factory=lambda: pendulum.now("UTC"))`

4. Line ~239 ActiveScenario (phase_started field):
   - `phase_started: datetime = Field(default_factory=datetime.utcnow)`
   - Change to: `phase_started: datetime = Field(default_factory=lambda: pendulum.now("UTC"))`

5. Line ~595 RecentAction:
   - `timestamp: datetime = Field(default_factory=datetime.utcnow)`
   - Change to: `timestamp: datetime = Field(default_factory=lambda: pendulum.now("UTC"))`

Also update method bodies that use datetime.utcnow():
- Line ~179 cleanup_old_directives: `cutoff = datetime.utcnow()` -> `cutoff = pendulum.now("UTC")`
- Line ~279 create_normal_flow: `now = datetime.utcnow()` -> `now = pendulum.now("UTC")`
- Line ~301 advance_to_phase: `now = datetime.utcnow()` -> `now = pendulum.now("UTC")`
- Line ~404 is_phase_ready_to_advance: `now = datetime.utcnow()` -> `now = pendulum.now("UTC")`
- Line ~417 is_overdue: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~421 get_days_in_current_phase: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~426 get_total_cycle_days: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~529 record_action: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~942 did_agent_recently_act: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~979 record_action: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~987 is_new_day: `datetime.utcnow()` -> `pendulum.now("UTC")`

**In src/logging/models.py**, add `import pendulum` and replace:
- Line ~29 LLMLogEntry: `timestamp: datetime = Field(default_factory=datetime.utcnow)` -> `Field(default_factory=lambda: pendulum.now("UTC"))`
- Line ~72 JiraLogEntry: same change
- Line ~106 ErrorLogEntry: same change
- Line ~133 EventLogEntry: same change
- Line ~168 SessionLogEntry (started_at): same change

**In src/scenarios/sprint_scenario.py**, add `import pendulum` and replace:
- Line ~126: `self.executed_at = datetime.now()` -> `self.executed_at = pendulum.now("UTC")`
- Line ~194: `created_at: datetime = Field(default_factory=datetime.now)` -> `Field(default_factory=lambda: pendulum.now("UTC"))`
- Line ~200: `self.started_at = datetime.now()` -> `self.started_at = pendulum.now("UTC")`
- Line ~209: `self.completed_at = datetime.now()` -> `self.completed_at = pendulum.now("UTC")`
  </action>
  <verify>
Run: `grep -n "datetime.utcnow\|datetime.now()" src/state/models.py src/logging/models.py src/scenarios/sprint_scenario.py`
Should return empty (no matches).

Run: `python -c "from src.state.models import SimulationState; print('OK')"`
Run: `python -c "from src.logging.models import LLMLogEntry; print('OK')"`
Both should succeed.
  </verify>
  <done>
- All Pydantic default_factory calls use pendulum.now("UTC")
- All method bodies use pendulum.now("UTC")
- No datetime.utcnow() or naive datetime.now() calls in these files
- Imports updated to include pendulum
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate orchestrator, analyzer, and agents to Clock/pendulum</name>
  <files>
    - src/orchestrator/orchestrator.py
    - src/orchestrator/analyzer.py
    - src/agents/coordinator_agent.py
    - src/agents/release_manager_agent.py
  </files>
  <action>
**In src/orchestrator/orchestrator.py:**

1. Add imports at top:
   ```python
   import pendulum
   from src.time import Clock, RealClock
   ```

2. Modify ScenarioOrchestrator.__init__ to accept optional clock:
   ```python
   def __init__(
       self,
       jira_client,
       llm_service,
       personas: dict,
       templates: dict,
       settings: dict,
       clock: Clock = None,  # Add this parameter
   ):
       self.clock = clock or RealClock()  # Add this line
       # ... rest of existing init
   ```

3. Replace datetime.utcnow() calls with self.clock.now():
   - Line ~174: `tick_start = datetime.utcnow()` -> `tick_start = self.clock.now()`
   - Line ~326: `datetime.utcnow().isoformat()` -> `self.clock.now().isoformat()`

4. Replace datetime.now(timezone.utc) calls:
   - Line ~954: `datetime.now(timezone.utc).date()` -> `self.clock.today()`
   - Line ~993: `datetime.now(timezone.utc).date()` -> `self.clock.today()`

**In src/orchestrator/analyzer.py:**
Add `import pendulum` and replace:
- Line ~199: `"timestamp": datetime.utcnow().isoformat()` -> `"timestamp": pendulum.now("UTC").isoformat()`

**In src/agents/coordinator_agent.py:**
Add `import pendulum` and replace:
- Line ~183: `"timestamp": datetime.utcnow().isoformat()` -> `"timestamp": pendulum.now("UTC").isoformat()`

**In src/agents/release_manager_agent.py:**
Add `import pendulum` and replace:
- Line ~344: `now = datetime.utcnow()` -> `now = pendulum.now("UTC")`
- Line ~421: `now = datetime.utcnow()` -> `now = pendulum.now("UTC")`
- Line ~645: `datetime.utcnow()` -> `pendulum.now("UTC")`
  </action>
  <verify>
Run: `grep -n "datetime.utcnow\|datetime.now(" src/orchestrator/*.py src/agents/*.py`
Should return empty or only show legitimate datetime usage (not utcnow/now calls).

Run: `python -c "from src.orchestrator import ScenarioOrchestrator; print('OK')"`
Run: `python -c "from src.agents.release_manager_agent import ReleaseManagerAgent; print('OK')"`
Both should succeed.
  </verify>
  <done>
- ScenarioOrchestrator accepts Clock in constructor
- ScenarioOrchestrator uses self.clock.now() for tick timing
- Analyzer and agents use pendulum.now("UTC")
- No datetime.utcnow() calls remain in orchestrator or agents
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining files and wire Clock into main.py</name>
  <files>
    - src/main.py
    - src/state/simulation_state.py
    - src/services/jira_client.py
    - src/logging/writer.py
    - src/logging/database.py
  </files>
  <action>
**In src/main.py:**

1. Add imports at top:
   ```python
   import pendulum
   from src.time import Clock, RealClock, get_clock
   ```

2. Replace all datetime.now(timezone.utc) with pendulum.now("UTC"):
   - Line ~145: `now = datetime.now(timezone.utc)` -> `now = pendulum.now("UTC")`
   - Line ~177: `datetime.now(timezone.utc).date()` -> `pendulum.now("UTC").date()`
   - Line ~392 area: `current_time=datetime.now(timezone.utc)` -> `current_time=pendulum.now("UTC")`
   - Line ~569: `datetime.utcnow()` -> `pendulum.now("UTC")`
   - Line ~581: `datetime.utcnow()` -> `pendulum.now("UTC")`
   - Line ~699: `datetime.now(timezone.utc)` -> `pendulum.now("UTC")`
   - Line ~771: `datetime.now(timezone.utc)` -> `pendulum.now("UTC")`
   - Line ~1648: `datetime.now(timezone.utc).isoformat()` -> `pendulum.now("UTC").isoformat()`
   - Line ~1981: `datetime.now(timezone.utc).isoformat()` -> `pendulum.now("UTC").isoformat()`

3. Pass clock to ScenarioOrchestrator in trigger_simulation():
   ```python
   clock = RealClock()  # Or get_clock() from dependency
   orchestrator = ScenarioOrchestrator(
       jira_client=logged_jira,
       llm_service=logged_llm,
       personas=app.state.personas,
       templates=app.state.templates,
       settings=app.state.settings,
       clock=clock,  # Add this
   )
   ```

**In src/state/simulation_state.py:**
Add `import pendulum` and replace:
- Line ~202: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~208: `datetime.utcnow()` -> `pendulum.now("UTC")`

**In src/services/jira_client.py:**
Add `import pendulum` and replace:
- Line ~56: `datetime.utcnow()` -> `pendulum.now("UTC")`
- Line ~74: `datetime.utcnow()` -> `pendulum.now("UTC")`

**In src/logging/writer.py:**
Add `import pendulum` and replace:
- Line ~89: `started_at=datetime.utcnow()` -> `started_at=pendulum.now("UTC")`
- Line ~113: `datetime.utcnow()` -> `pendulum.now("UTC")`

**In src/logging/database.py:**
Add `import pendulum` and replace:
- Line ~403: `datetime.utcnow()` -> `pendulum.now("UTC")`
  </action>
  <verify>
Full codebase check:
Run: `grep -rn "datetime.utcnow\|datetime\.now()" src/ --include="*.py" | grep -v "pendulum.now"`
Should return empty (no matches for direct datetime calls).

Run: `python -c "from src.main import app; print('main.py OK')"`
Run: `python -c "from src.services.jira_client import JiraClient; print('jira OK')"`
Both should succeed.
  </verify>
  <done>
- All files migrated to pendulum.now("UTC")
- Clock injected into ScenarioOrchestrator from main.py
- No datetime.utcnow() or naive datetime.now() calls in entire src/
- Application starts without import errors
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "datetime.utcnow" src/ --include="*.py"` returns empty
2. `grep -rn "datetime\.now()" src/ --include="*.py" | grep -v "pendulum"` returns empty
3. `python -c "from src.main import app; from src.orchestrator import ScenarioOrchestrator; print('All imports OK')"`
4. `python -c "import pendulum; from src.state.models import RecentAction; r = RecentAction(agent_id='test', agent_name='Test', action_type='test'); print(r.timestamp.timezone_name)"` prints "UTC"
</verification>

<success_criteria>
- Zero datetime.utcnow() calls in src/
- Zero naive datetime.now() calls in src/
- All Pydantic models use pendulum.now("UTC") for timestamps
- Clock injected into ScenarioOrchestrator
- Application imports and starts without errors
- Timestamps are timezone-aware UTC
</success_criteria>

<output>
After completion, create `.planning/phases/01-time-infrastructure/01-03-SUMMARY.md`
</output>
