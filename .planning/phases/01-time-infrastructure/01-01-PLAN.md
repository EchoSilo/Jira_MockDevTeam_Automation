---
phase: 01-time-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/time/__init__.py
  - src/time/clock.py
  - tests/test_clock.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "Developer can import Clock, RealClock, FakeClock from src.time"
    - "FakeClock.now() returns the frozen time passed to constructor"
    - "FakeClock.advance(hours=1) moves frozen time forward by 1 hour"
    - "RealClock.now() returns timezone-aware UTC datetime"
    - "All Clock implementations return pendulum.DateTime objects"
  artifacts:
    - path: "src/time/__init__.py"
      provides: "Clock module exports"
      exports: ["Clock", "RealClock", "FakeClock", "get_clock"]
    - path: "src/time/clock.py"
      provides: "Clock protocol and implementations"
      min_lines: 50
    - path: "tests/test_clock.py"
      provides: "Clock unit tests"
      min_lines: 30
  key_links:
    - from: "src/time/__init__.py"
      to: "src/time/clock.py"
      via: "re-exports"
      pattern: "from \\.clock import"
---

<objective>
Create the Clock abstraction module with RealClock and FakeClock implementations using Pendulum for timezone-aware datetime handling.

Purpose: This is the foundation for all time handling in the system. The Clock protocol enables dependency injection so tests can freeze/advance time deterministically without monkeypatching.

Output: `src/time/` module with Clock protocol, RealClock (production), FakeClock (testing), and unit tests. Pendulum added to requirements.txt.
</objective>

<execution_context>
@C:\Users\Jamal\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Jamal\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-time-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pendulum dependency and create Clock module</name>
  <files>
    - requirements.txt
    - src/time/__init__.py
    - src/time/clock.py
  </files>
  <action>
1. Add `pendulum>=3.1.0` to requirements.txt (after existing datetime-related packages if any, otherwise near the top with other core deps).

2. Create `src/time/__init__.py`:
```python
"""Time infrastructure module for timezone-aware datetime handling."""
from .clock import Clock, RealClock, FakeClock, get_clock

__all__ = ["Clock", "RealClock", "FakeClock", "get_clock"]
```

3. Create `src/time/clock.py` with:

- `Clock` Protocol with methods:
  - `now() -> pendulum.DateTime` - current time in UTC
  - `today() -> pendulum.Date` - current date in UTC

- `RealClock` class implementing Clock:
  - `now()` returns `pendulum.now("UTC")`
  - `today()` returns `pendulum.now("UTC").date()`

- `FakeClock` class implementing Clock:
  - Constructor accepts `frozen_time: pendulum.DateTime`
  - `now()` returns the frozen time
  - `today()` returns the frozen time's date
  - `advance(**kwargs)` method that adds time delta (e.g., `advance(hours=1, days=2)`)
  - `set_time(new_time: pendulum.DateTime)` method for jumping to specific time

- `get_clock()` function that returns a RealClock instance (for FastAPI dependency injection later)

Use typing.Protocol for the Clock interface. All datetime returns must be pendulum.DateTime (timezone-aware UTC).
  </action>
  <verify>
Run: `python -c "from src.time import Clock, RealClock, FakeClock; print('Import OK')"`
Verify Pendulum installed: `pip install -r requirements.txt && python -c "import pendulum; print(pendulum.__version__)"`
  </verify>
  <done>
- Clock protocol defined with now() and today() methods
- RealClock returns pendulum.now("UTC")
- FakeClock stores frozen time and has advance() method
- All exports accessible from src.time
- Pendulum 3.1.0+ in requirements.txt
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Clock unit tests</name>
  <files>
    - tests/test_clock.py
  </files>
  <action>
Create `tests/test_clock.py` with pytest tests:

1. `test_real_clock_returns_utc`:
   - Create RealClock instance
   - Call now()
   - Assert result has timezone_name == "UTC"
   - Assert result is instance of pendulum.DateTime

2. `test_real_clock_today_returns_date`:
   - Create RealClock instance
   - Call today()
   - Assert result is instance of pendulum.Date or date

3. `test_fake_clock_returns_frozen_time`:
   - Create frozen time: `pendulum.datetime(2026, 1, 28, 10, 0, 0, tz="UTC")`
   - Create FakeClock with frozen time
   - Assert clock.now() == frozen_time
   - Assert clock.now().timezone_name == "UTC"

4. `test_fake_clock_advance`:
   - Create FakeClock at specific time
   - Call advance(hours=2)
   - Assert now() is 2 hours later
   - Call advance(days=1)
   - Assert now() is 1 day + 2 hours later

5. `test_fake_clock_set_time`:
   - Create FakeClock at one time
   - Call set_time() with different time
   - Assert now() returns new time

6. `test_fake_clock_today`:
   - Create FakeClock at specific datetime
   - Assert today() matches the date portion

Import pendulum and the Clock classes. Use standard pytest assertions.
  </action>
  <verify>
Run: `pytest tests/test_clock.py -v`
All 6 tests must pass.
  </verify>
  <done>
- 6 tests covering RealClock and FakeClock behavior
- Tests verify timezone awareness (UTC)
- Tests verify advance() and set_time() work correctly
- All tests pass
  </done>
</task>

</tasks>

<verification>
1. `pip install -r requirements.txt` succeeds with pendulum installed
2. `python -c "from src.time import Clock, RealClock, FakeClock, get_clock"` imports successfully
3. `pytest tests/test_clock.py -v` shows all tests passing
4. `python -c "from src.time import RealClock; c = RealClock(); print(c.now().timezone_name)"` prints "UTC"
</verification>

<success_criteria>
- Clock protocol exists with now() and today() methods
- RealClock implementation returns timezone-aware UTC datetimes via Pendulum
- FakeClock implementation allows freezing and advancing time
- All unit tests pass
- Module is importable from src.time
</success_criteria>

<output>
After completion, create `.planning/phases/01-time-infrastructure/01-01-SUMMARY.md`
</output>
