---
phase: 04-adaptive-pathfinding-chaos-injection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/chaos/__init__.py
  - src/chaos/models.py
  - config/settings.yaml
  - tests/test_chaos_models.py
autonomous: true

must_haves:
  truths:
    - "RandomEvent model validates event types, severity, and timestamps"
    - "ChaosConfig loads per-event-type probabilities from settings.yaml"
    - "Event types include production_outage, urgent_bug, team_absence, external_blocker, priority_shift, scope_change"
  artifacts:
    - path: "src/chaos/models.py"
      provides: "RandomEvent and ChaosConfig dataclasses"
      contains: "class RandomEvent"
    - path: "src/chaos/__init__.py"
      provides: "Public exports for chaos module"
      exports: ["RandomEvent", "ChaosConfig", "ChaosEventType"]
    - path: "config/settings.yaml"
      provides: "random_events configuration section"
      contains: "random_events:"
  key_links:
    - from: "src/chaos/models.py"
      to: "config/settings.yaml"
      via: "ChaosConfig loads from YAML"
      pattern: "event_probabilities"
---

<objective>
Create data models for chaos injection including RandomEvent, ChaosConfig, and ChaosEventType enum.

Purpose: Provide strongly-typed data structures that define chaos events with configurable per-event-type probabilities.

Output: Tested chaos models with configuration loading from settings.yaml
</objective>

<execution_context>
@C:\Users\Jamal\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Jamal\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adaptive-pathfinding-chaos-injection/04-RESEARCH.md
@config/settings.yaml
</context>

<feature>
  <name>Chaos Event Models and Configuration</name>
  <files>src/chaos/models.py, src/chaos/__init__.py, config/settings.yaml, tests/test_chaos_models.py</files>
  <behavior>
    ChaosEventType enum with 6 event types:
    - production_outage
    - urgent_bug
    - team_absence
    - external_blocker
    - priority_shift
    - scope_change

    RandomEvent dataclass:
    - event_id: str (auto-generated uuid[:8])
    - event_type: ChaosEventType
    - triggered_at: pendulum.DateTime
    - affected_tickets: list[str]
    - affected_agents: list[str] (for team_absence)
    - description: str
    - severity: str (low/medium/high/critical)
    - duration_ticks: int (default 1, for multi-tick events like absence)
    - details: dict (event-specific parameters)

    ChaosConfig dataclass:
    - enabled: bool
    - base_event_chance: float (0.1 = 10% any event per tick)
    - event_probabilities: dict[str, float] mapping event_type -> relative weight
    - Default weights: production_outage=0.05, urgent_bug=0.10, team_absence=0.03, external_blocker=0.15, priority_shift=0.12, scope_change=0.05
    - load_from_settings() classmethod loads from config/settings.yaml

    Tests (run pytest tests/test_chaos_models.py):
    - RandomEvent validates severity enum
    - RandomEvent auto-generates event_id
    - ChaosConfig loads from YAML with defaults
    - ChaosConfig handles missing random_events section gracefully
    - Event probabilities must be 0.0-1.0 range
  </behavior>
  <implementation>
    1. Create src/chaos/ directory with __init__.py
    2. Create ChaosEventType enum with 6 types
    3. Create RandomEvent dataclass with fields above
    4. Create ChaosConfig dataclass with load_from_settings()
    5. Add random_events section to config/settings.yaml with default probabilities
    6. Create tests for model validation and config loading
  </implementation>
</feature>

<verification>
- `pytest tests/test_chaos_models.py -v` passes all tests
- `python -c "from src.chaos import RandomEvent, ChaosConfig, ChaosEventType; print('imports OK')"` succeeds
- settings.yaml contains `random_events:` section with all 6 event types
</verification>

<success_criteria>
- RandomEvent model with validated fields (event_type, severity, timestamps)
- ChaosConfig loads per-event-type probabilities (CHAOS-02, CONFIG-03)
- ChaosEventType enum covers all 6 event types (CHAOS-01)
- Tests verify model validation and config loading
</success_criteria>

<output>
After completion, create `.planning/phases/04-adaptive-pathfinding-chaos-injection/04-01-SUMMARY.md`
</output>
